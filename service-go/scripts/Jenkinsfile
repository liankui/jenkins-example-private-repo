pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        sh 'echo "build starting"'
        dir('../cmd') {
            // some block
        }
        sh 'go run main.go'
      }
    }
    stage('execute') {
      steps {
        echo 'done'
      }
    }
  }
}
