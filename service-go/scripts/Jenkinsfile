pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        sh 'echo "build starting"'
        sh 'cd ../cmd'
        sh 'go run main.go'
      }
    }
    stage('execute') {
      steps {
        echo 'done'
      }
    }
  }
}
