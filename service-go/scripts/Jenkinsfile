pipeline {
  agent any
  tools { go '1.17.6' }

  stages {
    stage('build') {
      steps {
        echo 'build starting'
        sh "go version"
      }
    }
    stage('execute') {
      steps {
        echo 'done'
      }
    }
  }
}
